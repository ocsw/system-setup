### first install git/hg, vim

mkdir -p ~/.ssh
chmod 700 ~/.ssh
touch ~/.ssh/config
chmod 600 ~/.ssh/config
### remember to add a passphrase!
ssh-keygen -a 100 -t ed25519 -f ~/.ssh/github_ed25519
cat >> ~/.ssh/config <<-EOF
	Host github.com
		IdentityFile ~/.ssh/github_ed25519
	EOF

### global git
git config --global user.name "Danielle Zephyr Malament"
git config --global user.email EMAIL  # work or personal, depending
git config --global core.pager less
git config --global core.excludesfile ~/.gitignore_global
echo ".vscode" >> ~/.gitignore_global  # for GUI systems

### global hg (~/.hgrc)
[ui]
username = Danielle Zephyr Malament <EMAIL>  # work or personal, depending

[pager]
pager = less

### for each personal repo:
cd $REPO
git config user.name "Danielle Zephyr Malament"
git config user.email danielle.malament@gmail.com

### vim
mkdir ~/.vim
cd ~/.vim
git clone https://github.com/tpope/vim-pathogen.git
mkdir autoload
cd autoload
ln -s ../vim-pathogen/autoload/pathogen.vim .
cd ..
mkdir bundle
cd bundle
git clone https://github.com/nvie/vim-flake8.git
git clone https://github.com/wincent/command-t.git
git clone https://github.com/altercation/vim-colors-solarized.git
git clone https://github.com/terryma/vim-multiple-cursors.git
git clone https://github.com/scrooloose/nerdtree.git
git clone https://github.com/tpope/vim-surround.git
cd ~

### python
# see python.txt
