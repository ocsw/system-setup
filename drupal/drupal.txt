install:
  enable mod_rewrite
  either of:
    AllowOverride All
    add rewrites to apache config

  create database

  cd /root
  wget http://ftp.drupal.org/files/projects/drupal-7.22.tar.gz
  tar xvzf drupal-7.22.tar.gz
  mv drupal-7.22 SITEROOT
  chown -R root:root SITEROOT
  cd SITEROOT/sites/default
  cp default.settings.php settings.php
  chmod 777 .
  chmod 666 settings.php

  load site, go through installer
  
  chmod 755 .
  chmod 644 settings.php

  crontab:
    45 * * * * wget -O - -q -t 1 URLWITHKEY

modules:
  apt-get install drush
  cd SITEROOT

  drush dis toolbar overlay dashboard

  drush dl module_filter admin_menu devel fpa features strongarm \
           advanced_help ctools views views_bulk_operations node_clone \
           entityreference rules content_access

  drush en module_filter admin_menu admin_menu_toolbar devel devel_generate \
           fpa features strongarm advanced_help ctools views views_ui \
           views_bulk_operations clone entity entityreference entity_token \
           rules rules_admin content_access

  #panels
  #eva
  #draggableviews
  #views_slideshow
  #views_accordion
  #globalredirect
  #term_merge
  #uniqueness
  #unique_field
  #page_theme
  #content_theme

  #skinr -> skinr, skinr_ui
  #fusion
  #acquia_prosper -> color, fluid, turn off blocks on big views

config:
  turn on clean URLs
  perms for anon users
  disable visitor account creation
  #uniqueness (search), title, description, message
